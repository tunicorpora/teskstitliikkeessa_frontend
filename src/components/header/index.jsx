import React from 'react';
import styles from './styles.scss';

export default () => (
  <div className={styles.pageHeader}>
    <div className={styles.innerHeader}>
      <h1>Tekstit liikkeessä -tietokanta</h1>
    </div>
  </div>
);
